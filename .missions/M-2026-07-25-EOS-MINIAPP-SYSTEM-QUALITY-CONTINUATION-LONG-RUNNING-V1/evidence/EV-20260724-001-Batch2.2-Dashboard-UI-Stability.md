# Evidence: Batch 2.2 — Dashboard UI 稳定性检查

**Mission**: M-2026-07-25-EOS-MINIAPP-SYSTEM-QUALITY-CONTINUATION-LONG-RUNNING-V1
**Phase**: 2
**Batch**: 2.2
**Executor**: Claude Code (CC)
**Date**: 2026-07-24
**Commit**: 5cec099

---

## 检查范围

`miniapp/pages/operation/dashboard/` 目录下 3 个文件：
- dashboard.js (205 行)
- dashboard.wxml (236 行)
- dashboard.wxss (370 行)

---

## 检查结果（5 项检查）

### 1. 空数据显示

**原有状态**：
- Tab 1/2 趋势图有空状态（`enrollmentTrend.length === 0`）✅
- Tab 1/2 指标区域无空状态引导 ❌
- Tab 3 两个图表都有空状态 ✅
- 无引导文案 ❌

**修复后**：
- Tab 1 添加空状态引导：📋 "暂无学员数据" + "添加学员后即可查看统计" ✅
- Tab 2 添加空状态引导：📚 "暂无课程数据" + "创建课程后即可查看统计" ✅
- 空状态仅在 `isEmpty === true` 时显示 ✅

### 2. 加载状态

**原有状态**：
- 首次加载骨架屏 ✅
- Tab 切换无 loading（设计合理，数据已加载）✅
- 时间切换有 `trendLoading` + 图表骨架屏 ✅
- loading 状态正确重置 ✅

**修复**：无需修改，原有实现完整。

### 3. 错误恢复

**原有状态**：
- 全局错误：error 状态 + 错误卡片 + 重试按钮 ✅
- 趋势加载失败：仅 Toast 提示，无 UI 级重试 ❌

**修复后**：
- 新增 `trendError` 状态字段 ✅
- Tab 3 顶部添加趋势错误卡片 + "重新加载"按钮 ✅
- `retryTrend()` 方法调用 `loadTrendData()` ✅
- 错误信息友好："趋势数据加载失败" ✅

### 4. 角色限制

**原有状态**：
- onLoad 检查 role === 'Admin' || 'SuperAdmin' ✅
- 非管理员设置 `hasPermission: false` ✅
- 无权限 UI：🔒 "无访问权限" + "该页面仅对管理员开放" ✅
- "返回首页"按钮用 `wx.navigateBack()` — 可能无法回到首页 ❌

**修复后**：
- `goBack()` 改为 `wx.switchTab({ url: '/pages/index/index' })` ✅
- 确保无论导航栈如何都能回到首页 ✅

### 5. 数据展示

**原有状态**：
- 数字格式化：`{{totalStudents || 0}}` 处理 undefined/null ✅
- 百分比计算：`activeRate` 保留 1 位小数 ✅
- 趋势图：`heightPercent` 预计算，WXML 直接使用 ✅
- 时间切换：`switchDays()` 调用 `loadTrendData()` 更新数据 ✅

**修复**：无需修改，原有实现正确。

---

## 修改文件清单

| 文件 | 修改内容 |
|:-----|:---------|
| dashboard.js | +trendError 状态字段, loadTrendData 错误处理增强, goBack 改为 switchTab, +retryTrend 方法 |
| dashboard.wxml | Tab 1/2 空状态引导, Tab 3 趋势错误卡片+重试按钮 |
| dashboard.wxss | .empty-guide 系列样式（icon/text/hint） |

---

## 发现问题汇总

| 编号 | 问题 | 严重度 | 状态 |
|:-----|:-----|:-------|:-----|
| DASH-001 | Tab 1/2 指标区域无空状态引导 | 中 | ✅ 已修复 |
| DASH-002 | 趋势加载失败仅 Toast，无 UI 级重试 | 中 | ✅ 已修复 |
| DASH-003 | "返回首页"按钮用 navigateBack 而非 switchTab | 低 | ✅ 已修复 |

**总计**：3 个问题，全部已修复。

---

## 测试验证

- 后端测试：992 tests / 80 suites ALL PASS ✅
- 构建：未修改后端代码，无需重新构建
- 前端：小程序页面，需真机/模拟器验证

---

## 未修改项（确认正常）

- 加载状态管理（骨架屏 + trendLoading）
- 数据格式化（|| 0 + toFixed(1)）
- 趋势图渲染（heightPercent 预计算）
- 角色权限检查逻辑
- Tab 切换逻辑
- 时间范围切换逻辑
