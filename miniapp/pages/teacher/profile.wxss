/* pages/teacher/profile.wxss */
.container {
  padding: 20rpx;
  padding-bottom: 40rpx;
  background: #FAF6F0;
  min-height: 100vh;
}

/* === 加载 & 错误状态 === */
.loading-state,
.error-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 40rpx;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid #EDE5DA;
  border-top-color: #D97B3E;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  margin-bottom: 20rpx;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.loading-text,
.error-text {
  font-size: 28rpx;
  color: #9B9084;
  margin-bottom: 20rpx;
}

.error-icon {
  font-size: 60rpx;
  margin-bottom: 20rpx;
}

.retry-btn {
  font-size: 28rpx;
  background: #D97B3E;
  color: #fff;
  border-radius: 16rpx;
  padding: 12rpx 40rpx;
  margin-top: 20rpx;
}

/* === 顶部个人信息 === */
.profile-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 50rpx 30rpx 40rpx;
  background: linear-gradient(135deg, #D97B3E 0%, #A64E1D 100%);
  border-radius: 16rpx;
  margin-bottom: 20rpx;
}

.avatar-area {
  margin-bottom: 20rpx;
}

.avatar-img {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  border: 4rpx solid rgba(255, 255, 255, 0.5);
}

.avatar-circle {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  border: 4rpx solid rgba(255, 255, 255, 0.5);
}

.avatar-text {
  font-size: 48rpx;
  font-weight: bold;
  color: #ffffff;
}

.profile-name {
  font-size: 38rpx;
  font-weight: bold;
  color: #ffffff;
  margin-bottom: 8rpx;
}

.profile-role {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.8);
  background: rgba(255, 255, 255, 0.2);
  padding: 4rpx 20rpx;
  border-radius: 20rpx;
}

/* === 基础信息卡片 === */
.info-card {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
}

.card-title {
  font-size: 30rpx;
  font-weight: bold;
  color: #2E2A25;
  margin-bottom: 20rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.info-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #FAF6F0;
}

.info-row:last-child {
  border-bottom: none;
}

.info-label {
  font-size: 28rpx;
  color: #6F675D;
}

.info-value {
  font-size: 28rpx;
  color: #2E2A25;
  font-weight: 500;
}

/* === 班级统计 === */
.stats-card {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
}

.stats-grid {
  display: flex;
  justify-content: space-around;
  align-items: center;
  padding: 20rpx 0;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 10rpx 20rpx;
}

.stat-number {
  font-size: 48rpx;
  font-weight: bold;
  color: #D97B3E;
  margin-bottom: 8rpx;
}

.stat-label {
  font-size: 24rpx;
  color: #9B9084;
}

/* === 教学概览 === */
.overview-card {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
}

.overview-row {
  display: flex;
  justify-content: space-around;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #FAF6F0;
}

.overview-row:last-child {
  border-bottom: none;
}

.overview-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 10rpx 20rpx;
}

.overview-number {
  font-size: 40rpx;
  font-weight: bold;
  color: #2E2A25;
  margin-bottom: 8rpx;
}

.overview-label {
  font-size: 24rpx;
  color: #9B9084;
}

/* === 最近课程 === */
.recent-card {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
}

.lesson-item {
  padding: 20rpx 0;
  border-bottom: 1rpx solid #FAF6F0;
}

.lesson-item:last-child {
  border-bottom: none;
}

.lesson-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10rpx;
}

.lesson-class {
  font-size: 28rpx;
  font-weight: bold;
  color: #2E2A25;
}

.lesson-status {
  font-size: 22rpx;
  padding: 4rpx 16rpx;
  border-radius: 12rpx;
}

.status-PLANNED {
  background: #FBEEE2;
  color: #D97B3E;
  border: 1rpx solid #FBEEE2;
}

.status-TEACHING {
  background: #FCF0DF;
  color: #D98E2B;
  border: 1rpx solid #FCF0DF;
}

.status-COMPLETED {
  background: #FBEEE2;
  color: #D97B3E;
  border: 1rpx solid #FBEEE2;
}

.status-CANCELLED {
  background: #FAF6F0;
  color: #9B9084;
  border: 1rpx solid #EDE5DA;
}

.lesson-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.lesson-topic {
  font-size: 26rpx;
  color: #6F675D;
}

.lesson-date {
  font-size: 24rpx;
  color: #9B9084;
}

/* === 快捷入口 === */
.quick-nav {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  display: flex;
  justify-content: space-around;
}

.nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 16rpx 20rpx;
}

.nav-icon {
  font-size: 48rpx;
  margin-bottom: 10rpx;
}

.nav-text {
  font-size: 24rpx;
  color: #6F675D;
}

/* === 退出登录 === */
.logout-section {
  margin-top: 40rpx;
  padding: 0 20rpx;
}

.logout-btn {
  font-size: 30rpx;
  background: #fff;
  color: #C4483F;
  border: 1rpx solid #C4483F;
  border-radius: 16rpx;
  padding: 20rpx 0;
}
