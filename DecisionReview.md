# Decision Review — Sprint 4.8

> **Review Date**: 2026-07-15
> **Reviewed By**: Self Governance Engine
> **Scope**: All ADRs and DEC files in docs/DecisionLog/

---

## Summary

| Document | Doc Status | Verdict | Action |
|----------|-----------|---------|--------|
| ADR-007 | PROPOSED | **needs-review** | Partially implemented, contradictions exist |
| ADR-008 | PROPOSED | **valid** | Correct, awaiting CTO approval |
| ADR-009 | ACCEPTED | **needs-review** | Implementation does not match decision |
| ADR-010 | PROPOSED | **needs-review** | Event class created prematurely |
| DEC-005 | ACCEPTED | **valid** | All 8 decisions faithfully implemented |

**Result**: 2 valid, 3 need-review, 0 deprecated.

---

## ADR-007: Shared Domain Enums

**Status**: PROPOSED → **needs-review**

**Decision**: Move multi-consumer enums to `common/enums/`. Keep single-consumer enums in domain modules.

**Findings**:
- Subject: ✅ Successfully migrated to `common/enums/subject.enum.ts`
- Gender: ❌ Not migrated (ADR says it should be)
- EnrollmentStatus: ⚠️ Moved to `common/enums/` but ADR lists it as single-consumer (should stay in domain)
- ADR still marked PROPOSED despite partial implementation

**Recommendation**: Update ADR to reflect actual state. Formalize the expanded scope (EnrollmentStatus moved to common). Get CTO approval.

---

## ADR-008: Unified Code Generator

**Status**: PROPOSED → **valid**

**Decision**: Create `UnifiedCodeGeneratorService` in `common/services/` to replace 4 duplicated generators.

**Findings**:
- No implementation has occurred
- All 4 old generators still exist and function correctly
- The duplication problem described in the ADR still exists
- Decision remains architecturally sound

**Recommendation**: No action needed. Keep as-is until CTO approves and a sprint is allocated.

---

## ADR-009: Enrollment Reactivation

**Status**: ACCEPTED → **needs-review**

**Decision**: When WITHDRAWN student re-enrolls, UPDATE existing record (don't INSERT new row). Preserve UNIQUE constraint.

**Findings**:
- Guard logic correctly detects existing WITHDRAWN enrollment
- But `enroll()` still creates `new EnrollmentEntity()` and calls `save()` → INSERT
- UNIQUE constraint on `(classCode, studentCode)` will cause runtime error
- No `reactivate()` method exists
- No audit trail for reactivation

**Recommendation**: This is a **latent bug**. Fix implementation to UPDATE existing record when re-enrolling a WITHDRAWN student. The decision is correct; the code is wrong.

---

## ADR-010: Attendance Event Ownership

**Status**: PROPOSED → **needs-review**

**Decision**: Introduce `attendance.confirmed` as prerequisite for `lesson.finished`. Event chain: attendance.confirmed → lesson.finished → Finance deduction.

**Findings**:
- Event class `attendance-confirmed.event.ts` exists (created prematurely)
- No emitter, no guard, no subscriber for this event
- `lesson.finished` still triggers directly from ARCHIVED status
- The decision is architecturally sound but unimplemented

**Recommendation**: Either fully implement (ACCEPTED + code) or remove the premature event class until decision is approved. Currently creates confusion.

---

## DEC-005: Teaching Domain Deep Modeling

**Status**: ACCEPTED → **valid**

**Decision**: 8 architectural decisions for Teaching Domain.

**Findings**: All 8 decisions are faithfully reflected in the codebase:
1. Course Is Pure Knowledge Product ✅
2. Contract Is the Financial Unit ✅
3. Schedule Embedded in Class ✅
4. Two-Phase Events for Financial Safety ✅
5. Lesson Is the Atomic Business Unit ✅
6. ChangeRequest Over Direct Edit ✅
7. Batch Lesson Generation ✅
8. Enrollment Carries Contract Reference ✅

**Recommendation**: No action needed. Strongest-conforming document.

---

## Action Items

| Priority | Item | Sprint |
|----------|------|--------|
| HIGH | Fix ADR-009 enrollment reactivation bug (INSERT → UPDATE) | Sprint 5 |
| MEDIUM | Update ADR-007 to reflect actual enum locations | Sprint 5 |
| MEDIUM | Decide ADR-010: fully implement or remove premature event class | Sprint 5 |
| LOW | Update ADR-007 and ADR-008 status to ACCEPTED if CTO approves | Sprint 5 |

---

*This review is generated by the Self Governance Engine. Do not modify ADRs directly — create change requests through the governance process.*
