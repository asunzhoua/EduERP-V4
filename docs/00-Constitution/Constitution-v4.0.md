# EduERP V4 — Architecture Constitution

> 版本：v4.0
> 状态：架构冻结
> 最后更新：2026-07-06

---

## EduOS V1.0（稳定版）

**Education Operating System - 教育运营操作系统**

> 让教育运营更简单，让管理更透明。

---

## 一、项目定位（Project Position）

### 不是什么？

不是：教培小程序、ERP、CRM、OA、财务软件。这些都只是功能。

### 它是什么？

它是一套：Education Operating System（教育运营操作系统）

> 所有人每天的工作，都围绕EduOS运转。

以后：教师、管理员、老板、家长、学生（未来）、AI，全部连接到这一套系统。

---

## 二、项目愿景（Vision）

打造一套：稳定、简单、真实、持续成长的教育运营平台。

不是功能最多，而是每天真正有人愿意打开。

---

## 三、设计哲学（Design Philosophy）

整个项目以后遵守下面八条原则。

### 第一原则：Reality First（真实优先）

系统只记录真实发生。例如：老师完成课程 → 系统自动扣课。不是管理员去点扣课。

### 第二原则：Event First（事件优先）

任何业务全部来源事件。例如：LessonFinished → 扣课 → 工资 → 积分 → 消息 → 统计 → 日志。以后新增任何功能都是监听事件，不是改旧代码。

### 第三原则：Document First（单据优先）

所有修改都是新增业务单据。例如：修改课时应该新增课时修正单。以后老板永远知道为什么改、谁改、什么时候改。

### 第四原则：Rule First（规则优先）

不要写 if teacher==张老师，不要写 数学50元。全部配置化：工资、课时、积分、续费提醒都走规则中心。以后老板自己改。

### 第五原则：Audit First（审计优先）

所有关键业务必须记录：谁、什么时候、为什么修改。以后永远能查。

### 第六原则：Simplicity First（简单优先）

以后任何功能先问：老师每天都会用吗？不会就删除。老板每天都会看吗？不会就隐藏。不要为了万分之一增加一万个按钮。

### 第七原则：Stable First（稳定优先）

速度第二，稳定第一。例如：宁可消息慢2秒，不能扣错课。工资永远不能错。

### 第八原则：Human First（人优先）

AI永远建议，不是决定。所有财务、工资、课时、退款最终都是人确认。

---

## 四、EduOS架构（重新定义）

```
                  EduOS V1.0

         ┌────────────────────┐
         │ Foundation Center │
         └────────────────────┘
                    │
         ┌────────────────────┐
         │ Operation Center  │
         └────────────────────┘
                    │
         ┌────────────────────┐
         │ Exception Center  │
         └────────────────────┘
                    │
         ┌────────────────────┐
         │ Approval Center   │
         └────────────────────┘
                    │
         ┌────────────────────┐
         │ Rule Center       │
         └────────────────────┘
                    │
         ┌────────────────────┐
         │ Insight Center    │
         └────────────────────┘
                    │
         ┌────────────────────┐
         │ Audit Center      │
         └────────────────────┘
                    │
         ┌────────────────────┐
         │ System Center     │
         └────────────────────┘
```

---

## 五、所有角色（重新定义）

### 老板

目标：一分钟知道机构怎么样。打开微信就是经营数据。

### 管理员

目标：处理业务。不用统计，系统自动统计。

### 老师

目标：今天给谁上课、今天赚多少钱、今天反馈什么。结束。

### 家长

目标：孩子今天有没有课、还剩多少课、老师评价、积分、兑换礼物、请假。结束。

### AI（未来）

目标：辅助。例如：提醒续费、提醒流失、提醒高风险学生。不能自动收费、不能自动退款。

---

## 六、开发铁律（Architect Constitution）

**以后所有开发人员（包括AI）必须遵守：**

### 数据安全
1. 禁止直接修改核心业务数据（必须通过业务单据或事件处理）。
2. 禁止硬编码业务规则（工资、课时、积分、提醒等均走规则中心）。
3. 禁止跨模块直接调用（统一通过事件总线通信）。
4. 禁止删除审计日志（日志只归档，不删除）。

### 开发流程
5. 任何新增功能，必须先更新文档，再写代码。
6. 一个 Sprint 只完成一个业务域，禁止跨域开发。
7. 任何对已冻结模块的修改必须走 Change Request（CR）。

### 业务规则（新增）
15. **业务对象必须先存在，业务动作才能开发。** 例如：先有 Student，再开发 Lesson，最后开发 Attendance。禁止先开发签到再补学生信息。
16. **所有资金相关业务必须由 LessonFinished 事件触发。** 任何地方都不能直接扣课。流程：签到 → LessonFinished → EventBus → Attendance → Finance → Points → Notification → Dashboard。
17. **任何业务只能修改自己的数据，不允许跨 Domain 修改数据库。** 例如：Attendance 不能 UPDATE Student，Finance 不能 UPDATE Lesson。全部发事件通知。
18. **所有用户看到的数据必须来自数据库，不允许页面自己计算业务结果。** 例如：剩余课时必须在后台计算后返回，不允许前端用总课时减已扣课时自行计算。
19. **Lesson 是整个 EduOS 唯一的业务时间轴。** 所有业务（工资、签到、反馈、请假、积分、财务）全部围绕 Lesson 展开，不围绕 Class 或 Course。Lesson 是系统中最小的业务原子单位。
20. **Every Money Must Have A Lesson.** 每一分钱都必须能追溯到一节课。任何涉及资金的业务记录必须关联到具体的 Lesson ID，确保资金流向完全可审计。
21. **每一个 Domain 必须公开自己的 Event，不允许其他 Domain 猜测业务。** 例如：Finance 不能猜测 Lesson 是否结束，必须收到明确的 LessonFinished 事件后才能执行扣课和工资计算。
22. **所有状态变化必须是单向的，不允许跳状态。** 例如：Lesson 不能从 Draft 直接跳 Finished，必须经过 Scheduled → Teaching → Completed → Finished → Archived。跳状态会导致审计漏洞和业务错误。
23. **任何自动计算结果，都必须能够重新计算（Replayable）。** 例如：工资规则修改后，必须能够通过 Replay LessonFinished 事件重新生成工资数据，而不是直接修改数据库。所有计算结果必须有可重复的推导路径。
24. **Skeleton First — 任何新的 Domain，必须先完成 Skeleton，再开始业务开发。** 第一阶段：建立目录、Module、Controller、Service、Repository、DTO、Entity 骨架。第二阶段：通过编译、Lint、Swagger 检查。第三阶段：冻结 Skeleton。第四阶段：再开始编写业务逻辑。
25. **One Domain At A Time — 任一时刻只能开发一个业务 Domain。** 当前 Domain 未完成 Gate、Release、Freeze 前，不允许进入下一个 Domain。如需修改已冻结 Domain，必须通过 Change Request（CR）流程，而不是直接修改代码。此规则适用于所有开发人员（包括 AI），是确保多 Domain 边界清晰的核心约束。

---

## 七、文档体系

```
编号 | 文档             | 状态
01   | PRD 产品需求说明书    | 已完成
02   | SAD 系统总体架构设计   | 待完成
03   | 数据库设计（ER + 字段） | 待完成
04   | 权限与角色设计        | 待完成
05   | 事件总线规范         | 待完成
06   | API 接口规范       | 待完成
07   | 微信小程序设计        | 待完成
08   | Web 后台设计       | 待完成
09   | 消息通知规范         | 待完成
10   | 财务与工资规则        | 待完成
11   | 测试与验收标准        | 待完成
12   | 运维、备份与恢复       | 待完成
```
